stages:
  - build
  - test

default:
  image: node:current-alpine
  cache:
    key: ${CI_COMMIT_SLUG}
    policy: pull
    paths:
      - node_modules
  before_script:
    - npm i

build api:
  stage: build
  script:
    - npm start

test api:
  stage: test
  script:
    - npm run test
